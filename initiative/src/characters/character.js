export default class Character {
    // name: str
    // initiative: int
    constructor(id, name, initiative, isPlayer) {
        this.id = id;
        this.name = name;
        this.initiative = initiative;
        this.isPlayer = isPlayer;
    }
}
